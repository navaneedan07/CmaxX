# Frontend API base URL.
# Leave empty to use same-origin requests (recommended when backend serves frontend in production).
# Example for local split dev:
# VITE_API_BASE_URL=http://localhost:3001
VITE_API_BASE_URL=
