{
  "name": "customer-success-agent-backend",
  "version": "1.0.0",
  "description": "Customer Success AI Agent backend — Hindsight + Groq + Express",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "dev:clean": "node server.js",
    "start:clean": "node server.js",
    "start:prod": "node server.js",
    "test": "vitest run --pool threads"
  },
  "dependencies": {
    "@vectorize-io/hindsight-client": "^0.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.4"
  }
}
