version: "3.9"

services:
  cmaxx:
    build:
      context: .
      dockerfile: Dockerfile
    image: cmaxx:latest
    env_file:
      - Backend/.env
    environment:
      NODE_ENV: production
      PORT: 3001
    ports:
      - "3001:3001"
    restart: unless-stopped
